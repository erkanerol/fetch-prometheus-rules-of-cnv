# Alert Rule Markdown Creator 

***Installation***
```
python3 -m venv ./venv
source ./venv/bin/activate
pip3 install -r requirements.txt
```


***Usage***

> Set environment variable`KUBECONFIG` and ensure you are able to access the cluster before running. 

```
python3 fetch.py
```